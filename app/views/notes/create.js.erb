$("#notes-list").prepend('<%= render partial: "notes/note", locals: { note: @note } %>');

$("#note-<%= @note.id %>").focus();
$("#note-<%= @note.id %>").effect("highlight", {}, 3000);

$("#note_body").val('');

$("#no-notes-message").hide();