document.getElementById('notes-area').dispatchEvent(
  new CustomEvent('bracegirdle:notes:create', 
    {
      detail: {
        body: '<%= j render partial: "notes/note", locals: { note: @note } %>',
        id: '<%= @note.id %>'
      }
    }
  )
);