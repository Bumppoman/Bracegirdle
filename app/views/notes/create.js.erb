$("#notes-list").prepend('<%= j render partial: "notes/note", locals: { note: @note } %>');

$(window).scrollTop($("#note-<%= @note.id %>").position().top);
$("#note-<%= @note.id %>").effect("highlight", {}, 3000);

$("#note_body").val('');

$("#no-notes-message").hide();