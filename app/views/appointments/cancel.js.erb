document.getElementById('appointments-index').dispatchEvent(
  new CustomEvent('bracegirdle:appointments:appointmentCancelled', 
    {
      detail: {
        appointmentId: '<%= @appointment.id %>',
        message: 'You have successfully cancelled this <%= @appointment_name %>'
      }
    }
  )
);