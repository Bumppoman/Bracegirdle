document.getElementById('board_meetings-show').dispatchEvent(
  new CustomEvent('bracegirdle:matters:matterUnscheduled', 
    {
      detail: {
        matterId: '<%= @matter.id %>',
        type: '<%= @matter.board_application_type %>'
      }
    }
  )
);