document.getElementById('matters-schedulable').dispatchEvent(
  new CustomEvent('bracegirdle:matters:matterScheduled', 
    {
      detail: {
        matterId: '<%= @matter.id %>'
      }
    }
  )
);