document.getElementById('attachments').dispatchEvent(
  new CustomEvent('bracegirdle:attachments:attachmentDestroyed',
    {
      detail: {
        attachmentId: '<%= @attachment.id %>'
      }
    }
  )
);