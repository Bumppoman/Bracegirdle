$('#attachment-<%= @attachment.id %>').remove();
$('#delete-attachment-confirm').modal('hide');
if ($('#attachments-list').children().length == 0) {
    $('#attachments-list').append('<%= content_tag(:div, "There are currently no notes.", id: "no-attachments-message") %>');
}
