<% content_for :title, @page_info[:title] %>
<% content_for :breadcrumbs, 
  breadcrumbs_helper(
    [
      [
        @page_info[:breadcrumbs], 
        self.send("board_applications_#{@board_application.type.downcase}_index_path")
      ], 
      'Upload new application'
    ]
  ) 
%>
<%= render partial: 'board_applications/raw_application_form', locals: { object: @board_application } %>