document.getElementById('notifications').dispatchEvent(
  new CustomEvent('bracegirdle:notifications:notificationRead', 
    {
      detail: {
        notificationId: '<%= @notification.id %>',
        unread: <%= @unread %>
      }
    }
  )
);